server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: ai-mock-interview-backend

  datasource:
    # Use PostgreSQL if available, otherwise H2 in-memory
    url: ${SPRING_DATASOURCE_URL:jdbc:h2:mem:interviewdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    username: ${SPRING_DATASOURCE_USERNAME:sa}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER:org.h2.Driver}

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: ${SPRING_JPA_DIALECT:org.hibernate.dialect.H2Dialect}
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

# JWT Configuration
app:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration-ms: 86400000 # 24 hours

# AI LangGraph Microservice integration URL
ai-service:
  url: ${AI_SERVICE_URL:http://localhost:8000}
